---@meta

---@class io.papermc.paper.registry.data.WolfVariantRegistryEntry: 
local WolfVariantRegistryEntry = {}
---@return io.papermc.paper.registry.data.client.ClientTextureAsset # the client texture asset.
function WolfVariantRegistryEntry.angryClientTextureAsset(self, ) end

---@return io.papermc.paper.registry.data.client.ClientTextureAsset # the client texture asset.
function WolfVariantRegistryEntry.wildClientTextureAsset(self, ) end

---@return io.papermc.paper.registry.data.client.ClientTextureAsset # the client texture asset.
function WolfVariantRegistryEntry.tameClientTextureAsset(self, ) end


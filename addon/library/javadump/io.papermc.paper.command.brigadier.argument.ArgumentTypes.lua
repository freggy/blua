---@meta

---@class io.papermc.paper.command.brigadier.argument.ArgumentTypes: 
local ArgumentTypes = {}
---@return <unresolved> # argument that takes one entity
function ArgumentTypes.entity(self, ) end

---@return <unresolved> # argument that takes multiple entities
function ArgumentTypes.entities(self, ) end

---@return <unresolved> # argument that takes one player
function ArgumentTypes.player(self, ) end

---@return <unresolved> # argument that takes multiple players
function ArgumentTypes.players(self, ) end

---@return <unresolved> # player profile arguments
function ArgumentTypes.playerProfiles(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.blockPosition(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.blockState(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.itemStack(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.itemPredicate(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.namedColor(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.component(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.style(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.signedMessage(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.scoreboardDisplaySlot(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.namespacedKey(self, ) end

---@return <unresolved> # 
function ArgumentTypes.key(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.integerRange(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.doubleRange(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.world(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.gameMode(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.heightMap(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.uuid(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.objectiveCriteria(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.entityAnchor(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.time(self, ) end

---@param mintime int The minimum time required for this argument.
---@return <unresolved> # argument
function ArgumentTypes.time(self, mintime) end

---@return <unresolved> # argument
function ArgumentTypes.templateMirror(self, ) end

---@return <unresolved> # argument
function ArgumentTypes.templateRotation(self, ) end

---@param registryKey io.papermc.paper.registry.RegistryKey the registry's key
---@return <unresolved> # argument
function ArgumentTypes.resource(self, registryKey) end

---@param registryKey io.papermc.paper.registry.RegistryKey the registry's key
---@return <unresolved> # argument
function ArgumentTypes.resourceKey(self, registryKey) end


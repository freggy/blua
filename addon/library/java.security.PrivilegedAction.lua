---@meta

---@class java.security.PrivilegedAction
local PrivilegedAction = {}
---@return T # a class-dependent value that may represent the results of the         computation. Each class that implements         {@code PrivilegedAction}         should document what (if anything) this value represents.
function PrivilegedAction.run() end

